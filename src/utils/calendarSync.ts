// src/utils/calendarSync.ts

export const syncToGoogleCalendar = (itinerary: any) => {
    // 這裡應該實現與Google Calendar API的集成
    console.log('Syncing to Google Calendar:', itinerary)
  }
  
  export const syncToAppleCalendar = (itinerary: any) => {
    // 這裡應該實現與Apple Calendar的集成
    console.log('Syncing to Apple Calendar:', itinerary)
  }